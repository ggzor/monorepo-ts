import { divide } from '@monorepo/core'

function App() {
  console.log(divide(100, 0))
  console.log(divide(100, 20))

  return <div>Check the console for live reload of core code</div>
}

export default App
